#include <KeyEventHandler.h>

/*
	In furture update, KeyEventHandler class will update with
	default key control GameObject settings rather than manually 
	inheritance each control setting for each GameObject 

*/
